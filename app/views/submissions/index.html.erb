<h1><%= t :my_submissions %></h1>

<table class="table">
  <thead>
  <tr>
    <th><%=t('submission_date')%></th>
    <th><%=t('exercise')%></th>
    <th><%=t('output')%></th>
  </tr>
  </thead>

  <tbody>
  <% @submissions.each do |submission| %>
      <tr>
        <td><%= status_span submission.status %> <%= link_to submission.created_at, [submission.exercise, submission] %></td>
        <td><%= link_to submission.exercise.title, exercise_path(submission.exercise) %></td>
        <td><%= submission.result_preview %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<%= paginate @submissions %>
<br>

