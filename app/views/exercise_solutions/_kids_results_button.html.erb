<div class="row">
  <div class="col-md-12">
    <div class="actions">
      <% if assignment.solved? %>
        <%= next_exercise_button(@exercise) || close_modal_button %>
      <% else %>
        <button class="btn btn-complementary btn-block submission-control" id="kids-btn-retry" data-bs-dismiss="modal" aria-label="<%= t :retry_exercise %>"> <%= t :retry_exercise %> </button>
      <% end %>
    </div>
  </div>
</div>
